import { ServerSrcDirective } from './server-src.directive';

describe('ServerSrcDirective', () => {
  it('should create an instance', () => {
    const directive = new ServerSrcDirective();
    expect(directive).toBeTruthy();
  });
});
